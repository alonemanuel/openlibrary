{
  "name": "music-library",
  "private": true,
  "version": "0.1.0",
  "description": "Your liked songs as a library you own — a Cloudflare Worker, plus an offline build pipeline.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npx wrangler dev --config worker/wrangler.toml",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test \"test/*.test.mjs\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "eslint": "^10.9.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.67.0"
  }
}
